---
import Layout from '../layouts/Layout.astro';
import Contacto from '../components/Contacto.astro';

const title = "Contacto - TDI Inmobiliaria";
const description = "¿Tienes dudas o quieres una cotización? Contáctanos para gestionar tu propiedad con expertos en corretaje, arquitectura, servicios legales y obras civiles.";

const urlHeroBg = "https://ficticia.com/img/hero-contacto-inmobiliaria.jpg";
---

<Layout title={title} description={description}>
  <!-- Hero Section Rediseñada -->
  <section class="hero-section" style={`background-image: linear-gradient(rgba(30, 41, 59, 0.7), rgba(30, 41, 59, 0.7)), url('${urlHeroBg}');`}>
    <div class="container">
      <div class="hero-content animate">
        <h1>Contáctanos</h1>
        <p>¿Tienes dudas o quieres una cotización? Nuestro equipo multidisciplinario está listo para asesorarte.</p>
        <a href="#contact-form" class="btn-primary">Iniciar Proyecto</a>
      </div>
    </div>
  </section>

  <!-- Tarjetas de Contacto Mejoradas -->
  <section class="contact-cards-section animate">
    <div class="container">
      <div class="contact-cards-grid">
        {[{
          icon: 'bi-telephone',
          title: 'Teléfono',
          text: '+56 9 5335 2667',
          link: 'tel:+56953352667',
          aria: 'Llámanos al +56 9 5335 2667'
        }, {
          icon: 'bi-envelope',
          title: 'Correo',
          text: 'contacto@tdiinmobiliaria.com',
          link: 'mailto:contacto@tdiinmobiliaria.com',
          aria: 'Envíanos un correo electrónico'
        }, {
          icon: 'bi-geo-alt',
          title: 'Oficina',
          text: 'Santiago, Chile',
          link: 'https://maps.google.com',
          aria: 'Ver ubicación en Google Maps',
          target: '_blank',
          rel: 'noopener noreferrer'
        }].map((item, idx) => (
          <a href={item.link} class="contact-card pro-card" aria-label={item.aria} target={item.target} rel={item.rel}>
            <div class="pro-card-icon"><i class={item.icon} aria-hidden="true"></i></div>
            <div class="card-info">
              <h3 class="card-title">{item.title}</h3>
              <p class="card-text">{item.text}</p>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- Formulario de Contacto Ampliado -->
  <section id="contact-form" class="contact-form-section animate">
    <div class="container">
      <div class="form-header">
        <h2>Envíanos tus requerimientos</h2>
        <p class="form-subtitle">Completa el formulario y un especialista se contactará contigo en menos de 24 horas.</p>
      </div>
      <Contacto />
    </div>
  </section>

 
</Layout>

<style>
  :root {
    --primary: #2A4D6E;
    --secondary: #FF9F43;
    --accent: #48DBB4;
    --dark: #1A2B3C;
    --light: #F8F9FA;
  }
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }
  .hero-section {
    min-height: 60vh;
    display: flex;
    align-items: center;
    background-size: cover;
    background-position: center;
    position: relative;
    color: white;
    padding: 4rem 0;
  }
  .hero-content {
    max-width: 700px;
    margin: 0 auto;
    text-align: center;
    z-index: 2;
    position: relative;
  }
  .hero-content h1 {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 1.5rem;
    line-height: 1.2;
    text-shadow: 0 2px 4px rgba(0,0,0,0.3);
  }
  .hero-content p {
    font-size: 1.25rem;
    color: rgba(255,255,255,0.9);
    margin-bottom: 2.5rem;
  }
  .btn-primary {
    display: inline-block;
    background: var(--secondary);
    color: white;
    padding: 1rem 2.5rem;
    border-radius: 50px;
    font-weight: 600;
    text-decoration: none;
    font-size: 1.1rem;
    transition: all 0.3s ease;
    border: none;
    box-shadow: 0 8px 24px rgba(255,159,67,0.15);
  }
  .btn-primary:hover {
    background: var(--primary);
    color: #fff;
    transform: translateY(-2px);
    box-shadow: 0 12px 32px rgba(42,77,110,0.15);
  }
  .contact-cards-section {
    background: var(--light);
    padding: 4rem 0;
  }
  .contact-cards-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 2rem;
    justify-items: center;
  }
  .pro-card {
    background: #fff;
    border-radius: 18px;
    box-shadow: 0 6px 24px rgba(42,77,110,0.10);
    padding: 2.5rem 2rem;
    text-align: center;
    text-decoration: none;
    color: var(--dark);
    display: flex;
    flex-direction: column;
    align-items: center;
    transition: transform 0.3s, box-shadow 0.3s, color 0.3s;
    min-width: 220px;
    max-width: 340px;
    border: 1.5px solid transparent;
  }
  .pro-card:hover {
    transform: translateY(-10px) scale(1.04);
    box-shadow: 0 16px 32px rgba(42,77,110,0.16);
    color: var(--primary);
    border-color: var(--accent);
  }
  .pro-card-icon {
    width: 64px;
    height: 64px;
    border-radius: 50%;
    background: linear-gradient(135deg, #2a7cf7 0%, #1fd1c7 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2.3rem;
    color: #fff;
    margin-bottom: 1.2rem;
    box-shadow: 0 2px 12px rgba(31,209,199,0.10);
    transition: background 0.3s;
  }
  .pro-card:hover .pro-card-icon {
    background: linear-gradient(135deg, #FF9F43 0%, #2a7cf7 100%);
    color: #fff;
  }
  .card-title {
    font-size: 1.3rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: var(--dark);
  }
  .card-text {
    color: #6c757d;
    font-size: 1.1rem;
    margin-bottom: 0;
  }
  .contact-form-section {
    background: #fff;
    padding: 5rem 0 4rem 0;
  }
  .form-header {
    text-align: center;
    max-width: 1000px;
    margin: 0 auto 3rem auto;
  }
  .form-header h2 {
    font-size: 2.3rem;
    color: var(--dark);
    margin-bottom: 1rem;
    font-weight: 700;
  }
  .form-header p {
    font-size: 1.1rem;
    color: #6c757d;
  }
  .form-wrapper {
    max-width: 1000px;
    margin: 0 auto;
    background: var(--light);
    padding: 2.5rem 2rem;
    border-radius: 20px;
    box-shadow: 0 8px 24px rgba(42,77,110,0.07);
  }
  .map-section {
    padding: 0 0 4rem 0;
  }
  .map-section iframe {
    width: 100%;
    height: 400px;
    border: none;
    border-radius: 16px;
    filter: grayscale(1) contrast(1.1);
    box-shadow: 0 8px 24px rgba(42,77,110,0.07);
  }
  /* Animación */
  .animate {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.8s cubic-bezier(.4,0,.2,1), transform 0.8s cubic-bezier(.4,0,.2,1);
  }
  .animate.visible {
    opacity: 1;
    transform: translateY(0);
  }
  @media (max-width: 992px) {
    .contact-cards-grid {
      grid-template-columns: 1fr 1fr;
    }
  }
  @media (max-width: 768px) {
    .hero-section {
      min-height: 40vh;
      padding: 2rem 0;
    }
    .hero-content h1 {
      font-size: 2.1rem;
    }
    .contact-cards-section {
      padding: 2rem 0;
    }
    .pro-form-wrapper {
      padding: 1.5rem 0.7rem;
    }
    .pro-card {
      padding: 1.5rem 0.7rem;
    }
    .form-header h2 {
      font-size: 1.5rem;
    }
    .form-wrapper {
      padding: 1.2rem 0.5rem;
    }
    .map-section {
      padding-bottom: 2rem;
    }
    .pro-form-wrapper button[type="submit"],
    .pro-form-wrapper input[type="submit"] {
      font-size: 1rem;
      padding: 0.9rem 1.2rem;
    }
  }
  @media (max-width: 576px) {
    .contact-cards-grid {
      grid-template-columns: 1fr;
    }
    .form-wrapper {
      padding: 0.5rem 0.2rem;
    }
  }
  .pro-form-wrapper {
    background: #fff;
    border-radius: 24px;
    box-shadow: 0 12px 32px rgba(42,77,110,0.10);
    padding: 3rem 2.5rem;
    max-width: 600px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    animation: fadeInUp 0.7s cubic-bezier(.4,0,.2,1);
  }
  .pro-form-wrapper form {
    display: flex;
    flex-direction: column;
    gap: 1.2rem;
  }
  .pro-form-wrapper label {
    font-weight: 600;
    color: var(--dark);
    margin-bottom: 0.3rem;
    font-size: 1.08rem;
    display: block;
  }
  .pro-form-wrapper input,
  .pro-form-wrapper textarea,
  .pro-form-wrapper select {
    width: 100%;
    padding: 0.9rem 1.1rem;
    border-radius: 12px;
    border: 1.5px solid #e0e6ed;
    font-size: 1.08rem;
    color: var(--dark);
    background: #f8f9fa;
    transition: border 0.3s, box-shadow 0.3s;
    outline: none;
    margin-bottom: 0.2rem;
    resize: vertical;
  }
  .pro-form-wrapper input:focus,
  .pro-form-wrapper textarea:focus,
  .pro-form-wrapper select:focus {
    border-color: var(--accent);
    box-shadow: 0 0 0 2px rgba(72,219,180,0.13);
    background: #fff;
  }
  .pro-form-wrapper button[type="submit"],
  .pro-form-wrapper input[type="submit"] {
    background: var(--secondary);
    color: #fff;
    font-weight: 700;
    font-size: 1.15rem;
    border: none;
    border-radius: 50px;
    padding: 1rem 2.5rem;
    margin-top: 0.5rem;
    box-shadow: 0 8px 24px rgba(255,159,67,0.13);
    transition: background 0.3s, transform 0.3s, box-shadow 0.3s;
    cursor: pointer;
    width: 100%;
    letter-spacing: 0.01em;
  }
  .pro-form-wrapper button[type="submit"]:hover,
  .pro-form-wrapper input[type="submit"]:hover {
    background: var(--primary);
    color: #fff;
    transform: translateY(-2px) scale(1.03);
    box-shadow: 0 12px 32px rgba(42,77,110,0.15);
  }
  .pro-form-wrapper .form-error {
    color: #ff5630;
    font-size: 0.98rem;
    margin-top: -0.5rem;
    margin-bottom: 0.5rem;
    font-weight: 500;
  }
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const animateElements = document.querySelectorAll('.animate');
    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          observer.unobserve(entry.target);
        }
        });
      }, { threshold: 0.2 });
    animateElements.forEach(element => {
      observer.observe(element);
    });
  });
</script>