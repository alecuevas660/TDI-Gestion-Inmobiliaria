---
// src/components/ComoTrabajamos.astro
import diagnostico from '../assets/img/diagnostico.jpg';
import estrategia from '../assets/img/estrategia.jpg'
import ejecucion from '../assets/img/ejecucion.jpg'
import seguimiento from '../assets/img/seguimiento.jpg'


---

<section class="process-section py-5">
  <div class="container">
    <h2 class="text-center fw-bold custom-title-color mb-2 fade-in-up">Nuestro Proceso</h2>
    <div class="title-underline custom-underline-color fade-in-up mb-5"></div>

    <div class="row g-4 justify-content-center">
      <div class="col-md-6 col-lg-3">
        <div class="process-card position-relative overflow-hidden rounded fade-in-up" data-delay="0" style={`background-image: url('${diagnostico.src}'); background-size: cover; background-position: center;`}>
          <div class="process-overlay d-flex flex-column justify-content-center align-items-center text-center p-3">
            <div class="process-icon-container mb-3">
              <div class="process-icon">
                <i class="bi bi-chat-dots"></i>
              </div>
            </div>
            <div class="process-title-container">
              <h3 class="fw-bold text-white mb-2 process-title">1. Diagnóstico</h3>
            </div>
            <div class="process-desc-container">
              <p class="process-description">Entendemos tus necesidades y objetivos a través de una conversación profunda y personalizada.</p>
            </div>
          </div>
        </div>
      </div>
      <div class="col-md-6 col-lg-3">
        <div class="process-card position-relative overflow-hidden rounded fade-in-up" data-delay="200" style={`background-image: url('${estrategia.src}'); background-size: cover; background-position: center;`}>
          <div class="process-overlay d-flex flex-column justify-content-center align-items-center text-center p-3">
            <div class="process-icon-container mb-3">
              <div class="process-icon">
                <i class="bi bi-lightbulb"></i>
              </div>
            </div>
            <div class="process-title-container">
              <h3 class="fw-bold text-white mb-2 process-title">2. Estrategia</h3>
            </div>
            <div class="process-desc-container">
              <p class="process-description">Desarrollamos un plan de acción adaptado a tus necesidades específicas y objetivos.</p>
            </div>
          </div>
        </div>
      </div>
      <div class="col-md-6 col-lg-3">
        <div class="process-card position-relative overflow-hidden rounded fade-in-up" data-delay="400" style={`background-image: url('${ejecucion.src}'); background-size: cover; background-position: center;`}>
          <div class="process-overlay d-flex flex-column justify-content-center align-items-center text-center p-3">
            <div class="process-icon-container mb-3">
              <div class="process-icon">
                <i class="bi bi-gear"></i>
              </div>
            </div>
            <div class="process-title-container">
              <h3 class="fw-bold text-white mb-2 process-title">3. Ejecución</h3>
            </div>
            <div class="process-desc-container">
              <p class="process-description">Implementamos la solución con un equipo especializado y tecnología de vanguardia.</p>
            </div>
          </div>
        </div>
      </div>
      <div class="col-md-6 col-lg-3">
        <div class="process-card position-relative overflow-hidden rounded fade-in-up" data-delay="600" style={`background-image: url('${seguimiento.src}'); background-size: cover; background-position: center;`}>
          <div class="process-overlay d-flex flex-column justify-content-center align-items-center text-center p-3">
            <div class="process-icon-container mb-3">
              <div class="process-icon">
                <i class="bi bi-check-circle"></i>
              </div>
            </div>
            <div class="process-title-container">
              <h3 class="fw-bold text-white mb-2 process-title">4. Seguimiento</h3>
            </div>
            <div class="process-desc-container">
              <p class="process-description">Acompañamiento continuo y ajustes necesarios para garantizar el éxito del proyecto.</p>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="row mt-5">
      <div class="col-lg-8 mx-auto text-center fade-in-up">
        <div class="process-cta">
          <h3 class="h4 mb-4">¿Listo para comenzar tu proyecto?</h3>
          <p class="mb-4">Agenda una consulta gratuita y descubre cómo podemos ayudarte a alcanzar tus objetivos.</p>
          <a href="/contacto" class="process-btn">Agendar Consulta</a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .process-section {
    background-color: #fff;
  }

  .custom-title-color {
    color: #003B73 !important;
  }

  .process-card {
    height: 300px;
    background: #003B73;
    transition: transform 0.3s ease-in-out;
    box-shadow: 0 4px 24px rgba(0,0,0,0.08);
  }

  .process-card:hover {
    transform: translateY(-8px) scale(1.02);
    box-shadow: 0 8px 32px rgba(0,0,0,0.13);
  }

  .process-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(180deg, rgba(0,59,115,0.68) 0%, rgba(0,59,115,0.32) 60%, rgba(0,59,115,0.18) 100%);
    opacity: 1;
    transition: background 0.3s;
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    align-items: center;
    text-align: center;
    height: 100%;
    padding: 2.2rem 1.2rem 2.2rem 1.2rem;
    gap: 0.7rem;
  }

  .process-card:hover .process-overlay {
    background: linear-gradient(180deg, rgba(0,59,115,0.82) 0%, rgba(0,59,115,0.38) 60%, rgba(0,59,115,0.22) 100%);
  }

  .process-icon-container {
    min-height: 80px;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100%;
  }

  .process-title-container {
    min-height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    padding-left: 10px;
    padding-right: 10px;
  }

  .process-desc-container {
    min-height: 60px;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    padding-left: 10px;
    padding-right: 10px;
  }

  .process-icon {
    width: 70px;
    height: 70px;
    background: rgba(255,255,255,0.1);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 4px 18px rgba(0,0,0,0.18);
    margin-bottom: 0.7rem;
  }

  .process-icon i {
    font-size: 2.1rem;
    color: #fff;
    text-shadow: 0 4px 18px rgba(0,0,0,0.35), 0 1.5px 8px rgba(0,0,0,0.25);
  }

  .process-title {
    font-size: 1.15rem;
    font-weight: 800;
    letter-spacing: 0.5px;
    color: #fff;
    text-shadow: 0 4px 18px rgba(0,0,0,0.35), 0 1.5px 8px rgba(0,0,0,0.25);
    margin-bottom: 0.4rem;
    margin-top: 0.2rem;
  }

  .process-description {
    color: #fff;
    font-size: 0.98rem;
    opacity: 0.98;
    margin-bottom: 0;
    margin-top: 0.1rem;
    text-shadow: 0 2.5px 12px rgba(0,0,0,0.32), 0 1px 4px rgba(0,0,0,0.18);
    line-height: 1.35;
  }

  .process-cta {
    background: white;
    padding: 3rem;
    border-radius: 1rem;
    box-shadow: 0 4px 24px rgba(0,0,0,0.08);
  }

  .process-btn {
    background: #ff5630;
    color: #fff;
    padding: 8px 22px;
    border-radius: 20px;
    font-weight: 600;
    text-decoration: none;
    font-size: 1rem;
    box-shadow: 0 2px 8px rgba(0,0,0,0.10);
    transition: background 0.2s, color 0.2s;
    display: inline-block;
    border: none;
  }

  .process-btn:hover {
    background: #d84315;
    color: #fff;
  }

  .title-underline {
    width: 80px;
    height: 4px;
    background: #003B73;
    margin: 0 auto;
    position: relative;
  }

  .title-underline.custom-underline-color::after {
    content: '';
    position: absolute;
    width: 40px;
    height: 4px;
    background: #003B73;
    right: -50px;
    opacity: 0.5;
  }

  .fade-in-up {
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.6s ease-out, transform 0.6s ease-out;
  }

  .fade-in-up.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .process-img {
    width: 70px;
    height: 70px;
    border-radius: 50%;
    overflow: hidden;
    box-shadow: 0 2px 8px rgba(0,59,115,0.10);
    margin-bottom: 0.5rem;
  }

  .process-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  @media (max-width: 767px) {
    .process-card {
      height: 220px;
    }
    .process-overlay {
      padding: 1.2rem 0.5rem 1.2rem 0.5rem;
      gap: 0.5rem;
    }
    .process-icon-container {
      min-height: 50px;
    }
    .process-title-container {
      min-height: 36px;
      padding-left: 6px;
      padding-right: 6px;
    }
    .process-desc-container {
      min-height: 40px;
      padding-left: 6px;
      padding-right: 6px;
    }
    .process-title {
      font-size: 0.98rem;
    }
    .process-description {
      font-size: 0.87rem;
    }
  }
</style>

<script>
  document.addEventListener('astro:page-load', () => {
    const observerOptions = {
      root: null,
      rootMargin: '0px',
      threshold: 0.1
    };

    const observer = new IntersectionObserver((entries, observer) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          const el = entry.target as HTMLElement;
          if (el.dataset.delay) {
            el.style.transitionDelay = `${el.dataset.delay}ms`;
          }
          observer.unobserve(entry.target);
        }
      });
    }, observerOptions);

    const fadeElements = document.querySelectorAll('.fade-in-up');
    fadeElements.forEach(el => observer.observe(el));
  });
</script>


